# Windows SMB storage for DBC

See `SMB_STORAGE.md` for setup and the one-click closeout command. The production design is local hot storage plus Windows SMB capacity/archive storage, with fail-closed mount checks and SHA-256 evidence verification.
