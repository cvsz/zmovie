# Runtime inputs

Required operator inputs are the Windows SMB server/share, a dedicated SMB account stored in `/root/.smb-zmovie`, and a real zMovie project ID for Gate 5. Provider may remain `auto`. No credential value belongs in Git.
