# Deployment note

The repository cannot know the Windows host IP/share name or SMB service-account credentials. Supply those only on DBC during installation. No runtime evidence in this repository should be interpreted as proof that the user's Windows share is already configured or reachable.
