# Final operator action

After merge, configure the real Windows share on DBC, run the SMB doctor, then run `runtime-closeout-smb.sh` with a real project ID. Only that host execution can close the SMB runtime evidence gap.
