# Summary

DBC keeps active production workload on the Ubuntu 300 GB local disk while Windows SMB holds durable media/archive capacity. The closeout wrapper verifies SMB, protects local free-space headroom, runs all five production gates, copies evidence, and verifies it with SHA-256.
