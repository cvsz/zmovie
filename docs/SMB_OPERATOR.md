# Operator handoff

Configure the Windows share and credentials on DBC, run `scripts/smb-storage-doctor.sh`, then execute `scripts/runtime-closeout-smb.sh --project <real-project> --provider auto`. Preserve the resulting local and SMB reports as production evidence.
