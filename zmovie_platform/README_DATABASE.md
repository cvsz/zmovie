# Database model

See the [documentation index](../docs/INDEX.md) for canonical guides and
cross-component procedures.

SQLite WAL mode is used for the default single-node deployment. The schema is normalized around projects and JSON payloads to keep migrations simple and local-first.
