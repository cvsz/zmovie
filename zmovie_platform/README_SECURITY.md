# Security baseline

See the [documentation index](../docs/INDEX.md) for canonical guides and
cross-component procedures.

Security baseline: non-root container, hardened systemd unit, PBKDF2 credentials, signed expiring tokens, CSP and browser security headers, bounded input sizes, rate-limit primitive, path normalization, and audit events.
