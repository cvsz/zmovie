# Render execution

See the [documentation index](../docs/INDEX.md) for canonical guides and
cross-component procedures.

Render execution is provider-agnostic. `mock` validates the full local pipeline; `webhook` delegates to a configured HTTP video-generation gateway while preserving the same job lifecycle.
