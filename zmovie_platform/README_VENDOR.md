# Vendor integration boundary

See the [documentation index](../docs/INDEX.md) for canonical guides and
cross-component procedures.

Provider credentials are never required for local dry runs. Vendor-specific configuration belongs in environment variables or an external gateway, not source control.
