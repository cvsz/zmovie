# Application entry point

See the [documentation index](../docs/INDEX.md) for canonical guides and
cross-component procedures.

`main:app` is the application entry point; it composes the legacy prompt API with the authenticated v2 platform, media preview, and publisher routes.
