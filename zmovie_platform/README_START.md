Start the HTTP app with `uvicorn main:app --host 0.0.0.0 --port 8080` or use the installer/container entry points.
API documentation is disabled by default; set `ZMOVIE_ENABLE_DOCS=true` when local API docs are required.
