# zMovie Platform Internals

This package contains the v2 production domain, persistence, providers, storyboard, QC, render orchestration, media assembly, authentication, and export modules. `app.py` remains the HTTP entry point and `zmovie.py` remains backward-compatible as the standalone prompt generator.
