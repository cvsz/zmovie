# API v2

See the [documentation index](../docs/INDEX.md) for canonical guides and
cross-component procedures.

API v2 is designed around the production manifest and render lifecycle while legacy `/api/generate` remains available for single-prompt generation.
