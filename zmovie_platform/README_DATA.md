# Persistent runtime data

See the [documentation index](../docs/INDEX.md) for canonical guides and
cross-component procedures.

Persistent runtime data lives under `data/` by default and is mounted as a Docker volume or owned by the native `zmovie` service account.
