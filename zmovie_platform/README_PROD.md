# Production readiness

See the [documentation index](../docs/INDEX.md) for canonical guides and
cross-component procedures.

Production readiness requires successful CI, healthy runtime probes, protected secrets, persistent data backups, and a real render gateway if final AI video generation is required.
