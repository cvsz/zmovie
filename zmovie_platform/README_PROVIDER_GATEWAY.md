Set `ZMOVIE_PROVIDER_WEBHOOK` and optional `ZMOVIE_PROVIDER_TOKEN` to connect zMovie to any HTTP render gateway. zMovie sends prompt, negative prompt, project/shot IDs, duration, aspect ratio and job metadata as JSON.
